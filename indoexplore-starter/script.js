const formBooking = document.getElementById("formBooking");

formBooking.addEventListener("submit", function (event) {
    // TODO:
    // 1. Ambil nilai dari semua input.
    // 2. Kosongkan seluruh pesan error lama.
    // 3. Validasi nama agar tidak kosong.
    // 4. Validasi WhatsApp: hanya angka dan minimal 10 digit.
    // 5. Validasi destinasi agar sudah dipilih.
    // 6. Validasi jumlah peserta minimal 1.
    // 7. Bila ada kesalahan, jalankan event.preventDefault().
    //
    // Petunjuk pola WhatsApp:
    // const polaWhatsapp = /^[0-9]{10,}$/;
    //
    // Contoh manipulasi DOM:
    // document.getElementById("errorNama").textContent = "Pesan error";
});
